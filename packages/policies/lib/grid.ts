/**
 * Re-exports the shared grid helpers so policies code can import from
 * `@/packages/policies/lib/grid` symmetrically with `packages/clients/lib/grid`.
 */
export {
  FIELD_SIZE_SPAN,
  FIELD_GRID_CLASSES,
} from "@/packages/template-builder/lib/grid";
